{
  "name": "adverb-checker-mcp",
  "version": "1.0.0",
  "description": "MCP server for detecting adverbs using Open English WordNet dictionary",
  "type": "module",
  "main": "dist/index.js",
  "module": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "smithery dev",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@smithery/sdk": "^0.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}